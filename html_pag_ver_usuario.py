import html_pag_ver_usuario_IMP

def gera(ses, usr1):
  """Retorna uma página HTML que mostra os dados do usuário {usr1}
  (que deve ser um objeto de tipo {Objeto_Usuario})."""
  return html_pag_ver_usuario_IMP.gera(ses, usr1)
