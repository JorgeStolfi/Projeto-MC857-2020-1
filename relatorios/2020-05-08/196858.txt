Acrescentando função buscar_por_origem em trecho.py:

1) criei uma referencia def busca_por_origem(cod) no arquivo trecho.py
2) como a função do arquivo objeto.py nao estava implementada, criei a referencia nesse mesmo arquivo def busca_por_campo(chave, val, cache, nome_tb, letra_tb, colunas):
3) em seguida criei o método busca_por_campo em trecho_IMP.py que retorna uma lista vazia
5) por fim, implementei a função buscar_por_origem(cod), que recebe como parâmetro uma string do código e retorna uma lista de identificadores dos trechos

Testando:

Como a função busca_por_campo não havia sido implementada, então nao foi possível realizar testes sobre o arquivo trecho.py e, em específico, a função buscar_por_origem.

Desse modo, criei uma test simples def test_busca_por_origem(cod): que retorna nada, mas printa uma mensagem de erro "Função {objeto.busca_por_campo} não implementada". 

