Ao rodar o teste para o módulo html_bloco_texto, foi encontrado um erro no arquivo testes/html_bloco_texto_TST.py, um erro na escrita do nome do módulo na chamada da função testa_modulo_html(). Foi corrigido mudando o primeiro argumento, de ht_bloco_texto para html_bloco_texto.
Em seguida, notei que o argumento {display} da função html_bloco_texto.gera() no mesmo arquivo de teste estava sendo passado como "Hello C", "Hello L" e "Hello R", argumentos não válidos para a propriedade display do CSS. Ao mudar o argumento de cria_linha() de "inline_block" para "inline-block", foi possível passar o mesmo argumento {disp} para os argumentos {texto} e {display} da função módulo html_bloco_texto.gera().
Após alterar cores, tamanho, peso e padding, conferi elemento por elemento dos argumentos fornecidos para a função e os elementos do html gerado, parecendo conferir com os elementos que deveriam ser gerados pelo módulo a mim encarregado.
