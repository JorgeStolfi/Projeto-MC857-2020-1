ISSUE 196858:
[U] Implementar o comando "ver_minhas_sessoes" e um botão "Minhas sessoes" no menu geral.
------------------------------------------

1) Modulo alterado:

	* html_menu_geral_IMP.py:

	Nesse módulo, eu adicionei o botao "Minhas sessoes" pra QUALQUER usuário (admin ou comum). Esse botao irá chamar o módulo "ver_minhas_sessoes".

	* processa_comando_http_IMP.py:

	Nesse módulo, fiz com que o comando ver_minhas_sessoes fosse processado caso for chamado.
	Para isso, importei o módulo comando_ver_minhas_sessoes e adicionei as seguintes linhas de código:

			    elif cmd == '/ver_minhas_sessoes':
      				# Quer ver sessões:
      				pag = comando_ver_minhas_sessoes.processa(ses, args)
	
	Desse modo, o botao "Minhas sessoes" irá processar o comando quando chamado.

2) Modulos implementados:

	*comando_ver_minhas_sessoes.py:

	Criei o módulo para especificar o que o comando faz e chamar a implementacao do comando.

	*comando_ver_minhas_sessoes_IMP.py:

	Criei o módulo que vai implementar de fato como o usuário verá suas próprias sessoes. Observa-se que esses dois módulos criados sao parecidos com o módulo comando_ver_sessoes e comando ver_minhas_sessoes_IMP. A única diferenca entre os comandos é que ele ve apenas os comandos da própria sessao do usuário logado.


3) Teste dos módulos: 

	* html_menu_geral:

	Rodei o teste. Nao obteve erros!

	* comando_ver_minhas_sessoes:

	Como o módulo foi implementado, tive que criar o arquivo comando_ver_minhas_sessoes_TST.py. Fiz os testes e nao obteve erros!