import html_pag_ver_compra_IMP

def gera(ses, cpr):
  """Retorna uma página HTML que mostra os dados do pedido de compra {cpr}
  (que deve ser um objeto de tipo {Objeto_Compra})."""
  return html_pag_ver_compra_IMP.gera(ses, cpr)
