import html_pag_ver_trecho_IMP

def gera(ses, tre):
  """Retorna uma página HTML que mostra os dados do trecho {tre}
  (que deve ser um objeto de tipo {Objeto_Trecho})."""
  return html_pag_ver_trecho_IMP.gera(ses, tre)
