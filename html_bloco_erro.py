import html_bloco_erro_IMP

def gera(msg):
  """O parâmetro {msg} deve ser uma string, que pode incluir quebras 
  de linha '\n'; ou uma lista ou tupla de tais strings.  Retorna um
  fragmento de HTML que contém essas mensagens, formatadas como
  mensagens de erro, e um botão "OK" que retorna para a página principal da loja."""
  return html_bloco_erro_IMP.gera(msg)
