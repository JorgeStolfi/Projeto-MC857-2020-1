import html_lista_de_usuarios_IMP

def gera(ids):
  """Dada uma lista {ids} de identificadores de usuarios,
  retorna o HTML da descrição resumida de todos esses usuarios.

  Cada descrição será gerada por
  {html_usuario.gera(user)},
  e terá um botão 'Ver' para mostrar a descrição detalhada."""
  return html_lista_de_usuarios_IMP.gera(ids)
