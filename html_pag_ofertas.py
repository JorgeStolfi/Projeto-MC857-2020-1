import html_pag_ofertas_IMP

def gera(ses, trcs, alterar):
  """Devolve uma página HTML com uma lista de trechos em oferta.
  
  A lista em si é formatada com {html_lista_de_poltronas.gera}.
  Cada poltrona terá um botao de "Ver"."""
  return html_pag_ofertas_IMP.gera(ses, trcs, alterar)
